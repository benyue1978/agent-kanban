{
  "name": "@agent-kanban/cli",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "bin": {
    "kanban": "./dist/index.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "pnpm build && vitest run"
  },
  "dependencies": {
    "@agent-kanban/contracts": "workspace:*",
    "commander": "^14.0.3"
  },
  "devDependencies": {
    "@types/node": "^24.6.0"
  }
}
